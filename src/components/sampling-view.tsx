import { View, Text } from 'react-native';

type Props = {};

const SamplingView: React.FC<Props> = () => {
  return (
    <View>
      <Text>SamplingView</Text>
    </View>
  );
};

export default SamplingView;
